# frozen_string_literal: true

RSpec.describe "Caching performance impact" do
  #   it "is provides good speed improvement" do
  #     static_time = Benchmark.realtime { StaticProduct.all.to_a }  #

  #     expect(time_without_cache).to be > time_without_cache
  #   end
  # end
end
